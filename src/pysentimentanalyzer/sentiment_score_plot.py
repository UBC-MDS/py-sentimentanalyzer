def sentiment_score_plot(df, col):
    """
    Creates a binned histogram showing count of reviews
    ranging from negative to positive sentiments score.

    Parameters
    ----------
    df : Pandas DataFrame
        DataFrame containing the reviews
    col: String
        Column name in the dataframe that contains the reviews 

    Returns
    -------
    Plot:
        A plot object

    Examples
    --------
    >>> plot = sentiment_score_plot(df, "reviews")
    >>> plt.show()
    """
    pass